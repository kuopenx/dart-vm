interface:
  display_name: "Dart VM Inspector"
  short_description: "Inspect Flutter network, UI, and VM extensions"
  default_prompt: "Use $dart-vm-inspect to inspect the running Flutter app with dart-vm."
